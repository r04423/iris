{
  "name": "iris-space-shooter",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "vite preview",
    "lint": "biome lint src/",
    "format": "biome format src/",
    "check": "biome check src/",
    "check:fix": "biome check --write src/",
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "iris-ecs": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0-beta",
    "vite": "^6.0.0"
  }
}
