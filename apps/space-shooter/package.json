{
  "name": "iris-space-shooter",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src/",
    "format": "biome format src/",
    "check": "biome check src/",
    "check:fix": "biome check --write src/"
  },
  "dependencies": {
    "iris-ecs": "workspace:*",
    "iris-inspect": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0-beta",
    "vite": "^6.0.0"
  }
}
